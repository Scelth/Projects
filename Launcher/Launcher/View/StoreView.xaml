<UserControl x:Class="Launcher.View.StoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Launcher.View"
             xmlns:VM="clr-namespace:Launcher.ViewModel"
             mc:Ignorable="d">
    <Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Width="373" Background="#FF303031">
            <ComboBox Width="187" Panel.ZIndex="1"/>
            <TextBox Width="186" Panel.ZIndex="1"/>
        </StackPanel>

        <Grid Grid.Column="0" Grid.Row="2" Background="Maroon">
            <Border BorderThickness="1 0 1 1" BorderBrush="#FF303031"/>
            <Border BorderThickness="0" Background="Black" Opacity="0.6"/>
        </Grid>
    </Grid>
</UserControl>
